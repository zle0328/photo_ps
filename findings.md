# 关键发现

- UI 图尺寸均为 621×1170，属于参考截图，代码应使用 rpx、安全区和弹性布局，不直接硬编码截图像素。
- 微信激励视频需要使用 `wx.createRewardedVideoAd`；只有 `onClose` 的 `isEnded` 为真时才授予生成资格。
- 广告可能无库存或加载失败，核心流程必须包含可解释的失败状态。
- 普通跳转小游戏只证明打开成功，不能证明完成试玩；本阶段不作为生成资格依据。
- 当前工作区不是 Git 仓库，无法使用 Git diff/提交作为验证证据。
